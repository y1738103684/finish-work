<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.nuc.finish.dao.GiftRecordMapper" >

    <insert id="insertGiftRecord">
        insert into gift_record_info(gift_id, user_id, number, update_time, create_time)
        values(#{vo.giftId}, #{vo.userId}, 1, now(), now())
    </insert>
</mapper>